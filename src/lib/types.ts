export type Car = {
  id?: string;
  name: string;
  car_name?: string; // To handle the new data format
  brand: string;
  description: string;
  imageUrl: string;
  imageHint: string;
  created_at: string;
};
